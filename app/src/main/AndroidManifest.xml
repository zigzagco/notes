<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes">
        <activity android:name=".ListActivity.EditActivity"></activity>
        <activity android:name=".ListActivity.ActivitySettings" />
        <activity android:name=".ListActivity.FavouritesSearch" />
        <activity android:name=".ListActivity.ActivitySearch" />
        <activity
            android:name=".ListActivity.AddNotesActivity"
            android:label="AddNotesActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ListActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>